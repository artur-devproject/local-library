## Local Library

The *Local Library* is developed to store information about books, copies of books, authors and other key information.

- The server is built on the basis of Express/Node.js.

- The client side is rendered using the PUG template engine.

- MongoDB is used as the main storage.